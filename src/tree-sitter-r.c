// Top level include of tree sitter C files so R knows to compile them.
// Easier than tweaking a Makevars to do it.
// The equivalent C++ file handles the C++ files.
#include "tree-sitter-r/parser.c"
